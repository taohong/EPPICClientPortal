<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tps.eppic.mobile.activity"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".MainPageActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".CardAndBalanceInqiryActivity" >
        </activity>
        <activity android:name="ClientSecurityProfileActivity"></activity>
        
        <!-- the activity is for client security profile -->
        <activity 
            android:label="@string/clientSecurityProfile"
            android:name=".ClientSecurityProfileActivity"></activity>
        <activity 
            android:label="@string/clientPhoto"
            android:name=".ClientPhotoActivity" />
        
        <!-- activity for client statment -->
        <activity
            android:label="@string/app_name"
            android:name=".ClientStmtPageActivity" >
        </activity>
    </application>

</manifest>